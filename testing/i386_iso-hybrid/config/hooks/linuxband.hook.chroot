#!/bin/bash

[[ ! -d /tmp/linuxband ]] && mkdir -p /tmp/linuxband; cd /tmp/linuxband
git clone -b master http://github.com/mortaromarcello/linuxband
cd linuxband
./autogen.sh
./configure && make && make install
rm -R -f -v /tmp/linuxband
